{
  "name": "honbako-sources",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "lint": "eslint --no-error-on-unmatched-pattern sources tests",
    "lint:fix": "eslint --no-error-on-unmatched-pattern sources tests --fix"
  },
  "jest": {
    "testEnvironment": "node",
    "passWithNoTests": true
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  }
}
